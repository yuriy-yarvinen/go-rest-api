# Custom Redis server config, mounted into the container in place of the
# image's default config (same pattern as configs/postgres/postgresql.conf).

# Persist to disk periodically so cached data survives a container restart
# (the cache is disposable, so this is a convenience, not a requirement).
save 60 1000

maxmemory 128mb
maxmemory-policy allkeys-lru
